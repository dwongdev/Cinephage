name: Release Please

on:
  push:
    branches: [dev]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  actions: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: google-github-actions/release-please-action@v4
        id: release
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          target-branch: dev
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

      - name: Trigger Docker build
        if: steps.release.outputs.release_created
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'docker-build.yml',
              ref: '${{ steps.release.outputs.tag_name }}',
              inputs: {
                release_tag: '${{ steps.release.outputs.tag_name }}',
                checkout_ref: '${{ steps.release.outputs.tag_name }}'
              }
            });

      - name: Trigger publish to API
        if: steps.release.outputs.release_created
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'publish-release.yml',
              ref: 'dev',
              inputs: {
                tag_name: '${{ steps.release.outputs.tag_name }}'
              }
            });

      - name: Trigger Discord announcement
        if: steps.release.outputs.release_created
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'release-discord.yml',
              ref: 'dev',
              inputs: {
                tag_name: '${{ steps.release.outputs.tag_name }}'
              }
            });

      - name: Trigger sync main to tag
        if: steps.release.outputs.release_created
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'sync-main-to-tag.yml',
              ref: 'dev',
              inputs: {
                tag_name: '${{ steps.release.outputs.tag_name }}'
              }
            });
